#include <bits/stdc++.h>
#define fastio() ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;

int main(void) {
    fastio();
    
    int n;
    cin >> n;
    int doojjonku = 900 + 60 + 600 + 170 + 160 + 110;

    cout << n / doojjonku;

    return 0;
}