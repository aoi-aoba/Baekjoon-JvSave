#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;

int n;

int main(void) {
    fastio;
    cin >> n;
    while (n--) {
        int temp;
        cin >> temp;
        cout << "yes\n";
    }

    return 0;
}