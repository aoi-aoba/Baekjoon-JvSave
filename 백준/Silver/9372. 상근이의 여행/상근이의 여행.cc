#include <bits/stdc++.h>
#define fastIO ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;

int t, n, m;

int main(void) {
    fastIO;
    cin >> t;
    while (t--) {
        cin >> n >> m;
        cout << n-1 << '\n';
        while (m--) {
            int a, b;
            cin >> a >> b;
        }
    }
}