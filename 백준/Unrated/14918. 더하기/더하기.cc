#include <bits/stdc++.h>
#define fastio() ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;

int a, b;

int main(void) {
    fastio();
    cin >> a >> b;
    cout << a + b << '\n';
    return 0;
}